# Meal-Subscription-And-Delivery-Scheduler
Users subscribe to meals like breakfast, lunch, or dinner on a daily or weekly basis. Each meal has a fixed  delivery time. When a user subscribes, the system calculates the next delivery time based on the current time. A  scheduler runs every hour, checks if any deliveries are due, and creates delivery records accordingly. 
